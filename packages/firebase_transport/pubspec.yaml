name: firebase_transport
description: Firebase Crashlytics/Analytics transport for revere logger.
version: 0.0.2

homepage: https://github.com/kumo01GitHub/revere
repository: https://github.com/kumo01GitHub/revere/packages/firebase_transport

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  firebase_crashlytics: ^3.4.0
  firebase_analytics: ^10.8.0
  revere: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
