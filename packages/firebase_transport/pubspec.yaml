name: firebase_transport
description: Firebase Crashlytics/Analytics transport for revere logger.
version: 1.0.0

homepage: https://github.com/kumo01GitHub/revere
repository: https://github.com/kumo01GitHub/revere/tree/main/packages/firebase_transport

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_crashlytics: ^5.1.0
  firebase_analytics: ^12.2.0
  revere: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
