## 1.1.0

* chore: update revere dependency to ^1.1.0.
* chore: upgrade `firebase_crashlytics` from `^3.4.0` to `^5.1.0` and `firebase_analytics` from `^10.8.0` to `^12.2.0`; update `parameters` type to `Map<String, Object>?` for API compatibility.
* chore: improve test coverage.
* chore: correct version number from `0.0.7` to `1.0.0`.
* ci: add `workflow_dispatch` trigger for manual execution; add `run_integration_tests` input (default: `false`) — integration tests are skipped on pull request and push.

## 1.0.0

* Initial release.
